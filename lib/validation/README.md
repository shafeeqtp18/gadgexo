# Validation schemas — Phase 2+

Form and API input validation (product data, prices, specifications,
etc.) lands here starting Phase 2, once there's a real schema to
validate against. Empty for now so the folder exists and is git-tracked.
