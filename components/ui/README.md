# Shared UI components — Phase 3

Buttons, inputs, cards, tables, modals, badges, and other reusable
primitives land here in Phase 3 (Design System + UI Foundation).
Empty for now so the folder exists and is git-tracked.
